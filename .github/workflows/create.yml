
name: Create 
on:
  workflow_dispatch:

jobs:
  builder: 
    environment: Games
    runs-on: bunsen
    steps:
      - uses: actions/checkout@v4
      - name: Create the ArgoCD projects 
        run: |
          make argocd_create
