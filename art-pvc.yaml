apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: art
  namespace: games
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn 
  resources:
    requests:
      storage: 128Mi
